<h3><%= t '.title' %></h3>

<table>
  <% for s in @project_states %>
    <tr>
      <td><%= link_to s.name, edit_project_state_path(s) %></td>
    </tr>
  <% end %>
</table>

<%= link_to t('project_states.new.title'), new_project_state_path %>
