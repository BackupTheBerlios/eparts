<h3><%= t '.title' %></h3>

<table>
  <% for o in @order_states %>
    <tr>
      <td><%= link_to o.name, edit_order_state_path(o) %></td>
    </tr>
  <% end %>
</table>

<%= link_to t('order_states.new.title'), new_order_state_path %>
